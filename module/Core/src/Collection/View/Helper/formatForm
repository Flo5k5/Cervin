<?php
// ./module/Application/src/Application/View/Helper/AbsoluteUrl.php
namespace Collection\View\Helper;

use Zend\View\Helper\AbstractHelper;
 
class formatForm extends AbstractHelper
{
    public function __invoke($form)
    {
        $returnv="";
        $form->prepare();
        $form->setAttribute('action', $this->url('artefact'));
        $form->setAttribute('method', 'post');
        $form->setAttribute('class','form-horizontal');
        $returnv .= $this->form()->openTag($form);
        
        foreach ($form as $element):

        endforeach;

        //$returnv .= 
        $returnv .= $this->form()->closeTag();
        return $returnv;
    }
}